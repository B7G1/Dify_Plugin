# Root Report Consolidation

Date: 2026-07-08

This directory collects loose report/render artifacts that previously lived at the repository root.

Purpose:

- keep the repository root focused on source, bootstrap, release, and project entrypoint files;
- keep report artifacts under the canonical `reports/` tree;
- preserve historical material without deleting or rewriting it.

This is a documentation hygiene location only. It is not an authoritative release status by itself; use `reports/README.md` and `reports/REPORT_MAP.md` for navigation.
