# Offline dependency set for the Dify plugin runtime (Python 3.12, Linux amd64).
# The local verification venv may use Python 3.11; runtime binary wheels must still provide cp312 Linux amd64 builds.
--no-index
--find-links=./wheels

dify_plugin==0.6.2
SQLAlchemy==2.0.51
PyMySQL==1.2.0
psycopg2-binary==2.9.12
pymssql==2.3.13
dmPython==2.5.32
dmSQLAlchemy==2.0.12
