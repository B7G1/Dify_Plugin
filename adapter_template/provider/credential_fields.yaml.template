# Design template only; copy fields into the real Provider schema after review.
database_type:
  value: <database>
host:
  type: text-input
  required: true
port:
  type: text-input
  required: true
username:
  type: text-input
  required: true
password:
  type: secret-input
  required: true
database:
  type: text-input
  required: true
connect_timeout:
  type: text-input
  required: false

# Document optional schema/service/SSL fields and safe defaults separately.
