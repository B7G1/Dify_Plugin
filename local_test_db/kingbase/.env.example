# Copy values into the current PowerShell process or a local untracked .env.
# Never guess a public image name: use the exact image loaded from vendor media.
KINGBASE_IMAGE=
KINGBASE_ADMIN_PASSWORD=
KINGBASE_HOST_PORT=54321
